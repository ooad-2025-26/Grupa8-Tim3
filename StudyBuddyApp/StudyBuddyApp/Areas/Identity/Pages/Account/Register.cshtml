@page
@model RegisterModel

@{
    ViewData["Title"] = "Registracija";
}

<div class="auth-page">
    <div class="auth-card">
        <div class="auth-header">
            <h1>Registracija</h1>
            <p>Kreiraj StudyBuddy nalog i pridruži se sesijama učenja.</p>
        </div>

        <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

            <div class="form-floating mb-3">
                <input asp-for="Input.Email"
                       class="form-control"
                       autocomplete="username"
                       aria-required="true"
                       placeholder="name@example.com" />
                <label asp-for="Input.Email">Email</label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>

            <div class="mb-3 password-field">
                <div class="form-floating">
                    <input asp-for="Input.Password"
                           class="form-control password-input"
                           id="passwordInput"
                           autocomplete="new-password"
                           aria-required="true"
                           placeholder="Npr. Studij1!" />
                    <label asp-for="Input.Password">Lozinka</label>
                </div>

                <button type="button" class="password-toggle" onclick="togglePassword(this)">
                    👁
                </button>

                <span asp-validation-for="Input.Password" class="text-danger"></span>
                <small class="password-help">
                    Lozinka treba imati najmanje 7 karaktera, jedno veliko slovo, jedan broj i jedan specijalni znak.
                </small>
                <span id="passwordRulesMessage" class="text-danger password-match-message"></span>
            </div>

            <div class="mb-3 password-field">
                <div class="form-floating">
                    <input asp-for="Input.ConfirmPassword"
                           class="form-control password-input"
                           id="confirmPasswordInput"
                           autocomplete="new-password"
                           aria-required="true"
                           placeholder="Potvrdi lozinku" />
                    <label asp-for="Input.ConfirmPassword">Potvrdi lozinku</label>
                </div>

                <button type="button" class="password-toggle" onclick="togglePassword(this)">
                    👁
                </button>

                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                <span id="passwordMatchMessage" class="text-danger password-match-message"></span>
            </div>

            <button id="registerSubmit" type="submit" class="w-100 btn btn-primary-sb">
                Registruj se
            </button>

            <div class="auth-links mt-3">
                <p>
                    Već imaš nalog?
                    <a asp-page="./Login" asp-route-returnUrl="@Model.ReturnUrl">Prijavi se</a>
                </p>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        function togglePassword(button) {
            const wrapper = button.closest(".password-field");
            const input = wrapper.querySelector(".password-input");

            if (input.type === "password") {
                input.type = "text";
                button.textContent = "🙈";
            } else {
                input.type = "password";
                button.textContent = "👁";
            }
        }

        const passwordInput = document.getElementById("passwordInput");
        const confirmPasswordInput = document.getElementById("confirmPasswordInput");
        const passwordRulesMessage = document.getElementById("passwordRulesMessage");
        const passwordMatchMessage = document.getElementById("passwordMatchMessage");
        const registerSubmit = document.getElementById("registerSubmit");

        function lozinkaIspunjavaPravila(lozinka) {
            const imaMinimalnuDuzinu = lozinka.length >= 7;
            const imaVelikoSlovo = /[A-Z]/.test(lozinka);
            const imaBroj = /[0-9]/.test(lozinka);
            const imaSpecijalniZnak = /[^A-Za-z0-9]/.test(lozinka);

            return imaMinimalnuDuzinu && imaVelikoSlovo && imaBroj && imaSpecijalniZnak;
        }

        function provjeriLozinku() {
            const lozinka = passwordInput.value;
            const potvrda = confirmPasswordInput.value;

            passwordRulesMessage.textContent = "";
            passwordMatchMessage.textContent = "";
            passwordInput.classList.remove("is-invalid");
            confirmPasswordInput.classList.remove("is-invalid");
            registerSubmit.disabled = false;

            if (potvrda.length === 0) {
                return;
            }

            if (!lozinkaIspunjavaPravila(lozinka)) {
                passwordRulesMessage.textContent = "Lozinka ne ispunjava sva pravila.";
                passwordInput.classList.add("is-invalid");
                registerSubmit.disabled = true;
                return;
            }

            if (lozinka !== potvrda) {
                passwordMatchMessage.textContent = "Lozinke se ne poklapaju.";
                confirmPasswordInput.classList.add("is-invalid");
                registerSubmit.disabled = true;
                return;
            }
        }

        confirmPasswordInput.addEventListener("input", provjeriLozinku);
        confirmPasswordInput.addEventListener("focus", provjeriLozinku);

        passwordInput.addEventListener("input", function () {
            if (confirmPasswordInput.value.length > 0) {
                provjeriLozinku();
            }
        });
    </script>
}